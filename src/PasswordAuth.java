import java.util.Scanner;

public class PasswordAuth {
    /*public PasswordAuth() {
        Scanner scanner = new Scanner(System.in);

        for (int i = 0; i < 3; i++) {
            System.out.println("Username: ");
            String username = scanner.nextLine();

            System.out.println("Password: ");
            String password = scanner.nextLine();

            // Her kan man se Username og Password for Mario og Alfonso
            if (username.equals("Mario") && password.equals("1234") ||
                    username.equals("Alfonso") && password.equals("1234")) {

                System.out.println(Farver.green + "ACCESS GRANTED!!" + Farver.reset);
                break;

                // Er kombinationen forkert mere end 3 gange, får du ikke adgang
            } else if (i == 2) {
                System.out.println(Farver.red + "ACCESS FULLY DENIED" + Farver.reset);
                System.exit(0);

                // Er kombinationen forkert får du lov til at prøve igen (max 3 forsøg)
            } else {
                System.out.println(Farver.red + "ACCESS DENIED!" + Farver.reset);
            }
        }
    }*/
}
